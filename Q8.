entrada = input("Digite os números separados: ")

numeros = list(map(float, entrada.split()))

maior_numero = numeros[0]

for numero in numeros:
    if numero > maior_numero:
        maior_numero = numero
print(f"O maior número digitado é: {maior_numero}")
